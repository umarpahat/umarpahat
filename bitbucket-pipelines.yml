pipelines:
  default:
    - step:
        name: Deploy to test
        deployment: test
        # trigger: manual  # Uncomment to make this a manual deployment.
        script:
          - echo "Deploying"
          - cat ./post-push.sh | ssh ubuntu@15.207.235.144
          - echo "deployed"
